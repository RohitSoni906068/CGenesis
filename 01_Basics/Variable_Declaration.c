#include<stdio.h>

int main(){
    int a; // Variable declaration
    a = 6; // Variable initialization

    int b=6; // Varible declaration+initialization

    int c=4,d=5; // Multiply Variable declaration+initialization
    printf("The output of this program is %d %d %d %d", a,b,c,d); // "%d" is a format specifier used in input/output functions like printf() and scanf().
    return 0;
}