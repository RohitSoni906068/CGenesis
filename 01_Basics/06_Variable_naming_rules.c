#include <stdio.h>
int main()
{
    //  Examples of Invalid Variable Names:

    int 1number; // We Cann't Start With Numbers,Symbol.

    char % grade; // contains invalid symbol

    float my score; // We Cann't contains space between

    int return; // We Cann't use Keyword

    //  Examples of Invalid Names:
    int number;
    float average_score; // You cann't start varibles name with spcial symbol but we use them between
    char grade1;
    double totalAmount;
}
/* Note-:

Rule                                        Description

Starts with                             A letter (A–Z, a–z) or _
Can contain                             Letters, digits, _
Can’t start with                        A digit
No                                      Special characters or spaces
Not allowed                             C keywords
Case-sensitive                          value ≠ Value

*/
